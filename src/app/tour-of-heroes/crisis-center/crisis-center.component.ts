import { Component } from "@angular/core";

@Component({
  selector: 'toh-crisis-center',
  templateUrl: './crisis-center.component.html',
  styleUrls: ['./crisis-center.component.css']
})
/*
 * Crisis center component:
 * is the root of the crisis center area, just as `AppComponent` is the root of the entire application
 * It's a shell for crisis management feature area, just as ´AppComponent´ is a shell to manage the high-level workflow.
 */
export class CrisisCenterComponent{}
