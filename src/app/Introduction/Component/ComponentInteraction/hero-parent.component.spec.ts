

describe('HeroParentComponent', () => {
  const heroNames = ['Dr IQ', 'Magneto', 'Sangoku']
  const masterName = 'Tortue Genial'

  it('should pass properties to children properly', async () => {
    const parent = element(by.tagName('app-hero-parent'));

  });
})
